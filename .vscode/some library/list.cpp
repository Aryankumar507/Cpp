#include<bits/stdc++.h>
using namespace std;
int main(){
    list<int> ls;
    ls.push_back(2);
    for(auto it:ls){
        cout<<it;
    }
    /*
    all the functions are same as vectors but here we can easily 
    push any number without using insert function which help us to 
    reduce time complexity;
    */ 
}

