#include<bits/stdc++.h>
using namespace std;
int main(){
    deque<int> dq;
    dq.push_back(2);
    dq.push_front(2);
    dq.emplace_back(4);
    dq.emplace_back(5);
    /*
    Similar to vector and list and we can use all function which is used in vector and list
    */
}